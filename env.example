# MBTI Processing Service - Environment Variables
# Copy this file to .env and update with your actual values

# Base directory paths (defaults work for Docker deployment)
PROJECT_BASE_DIR=/app
OUTPUT_DIR=/app/output
INPUT_DIR=/app/input
MEDIA_DIR=/app/backend/media

# OpenAI API Key (required for AI insights feature)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
# For production, set to specific domains separated by commas
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# For development, leave as * to allow all origins
CORS_ORIGINS=*

# Optional: Poppler path (if not in system PATH)
# POPPLER_PATH=/usr/bin

# Python path (usually set automatically in Docker)
# PYTHONPATH=/app/backend/src:/app/backend/src/MBTInfo:/app/backend/src/MBTInterpret

# Server port (default: 3000)
# PORT=3000

